{
  "name": "portfolio-root",
  "private": true,
  "scripts": {
    "postinstall": "cd landing && npm install && cd ../musicgen && npm install",
    "build": "rm -rf landing/dist && cd landing && npm run build && cd ../musicgen && npm run build && mv dist ../landing/dist/musicgen"
  }
}